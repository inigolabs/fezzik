package types

// TwoInput is expected to be bound to GraphQL input TwoInput
type TwoInput struct {
	TwoInt int
}

// Result is expected to be bound to GraphQL object type Result
type Result struct {
	Id string
}
